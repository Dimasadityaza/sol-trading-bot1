# Solana RPC Configuration
# Use a reliable RPC endpoint (Helius, QuickNode, Alchemy, etc.)
# For mainnet use: https://api.mainnet-beta.solana.com
# For devnet use: https://api.devnet.solana.com
RPC_ENDPOINT=https://api.mainnet-beta.solana.com
WS_ENDPOINT=wss://api.mainnet-beta.solana.com

# Database Configuration
# SQLite database path (relative to project root)
DATABASE_URL=sqlite+aiosqlite:///./sniper.db

# Security & Encryption
# IMPORTANT: Generate a strong, unique 32+ character encryption key for production
# This key is used to encrypt wallet private keys in the database
# Example: openssl rand -hex 32
ENCRYPTION_KEY=your-secure-encryption-key-min-32-characters-change-this

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Telegram Notifications
# Set these to enable Telegram alerts for trades and bot status
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: Advanced RPC Settings
# Use premium RPC providers for better performance
# HELIUS_API_KEY=
# QUICKNODE_ENDPOINT=
# ALCHEMY_API_KEY=

# Trading Configuration (Optional - can be set via UI)
# Default trading parameters
DEFAULT_SLIPPAGE=1.0
DEFAULT_PRIORITY_FEE=0.0001
DEFAULT_BUY_AMOUNT=0.1

# Sniper Configuration (Optional - can be set via UI)
MIN_LIQUIDITY=5.0
MIN_SAFETY_SCORE=70
MAX_BUY_TAX=10.0
MAX_SELL_TAX=10.0
REQUIRE_MINT_RENOUNCED=true
REQUIRE_FREEZE_RENOUNCED=true
