# Network Configuration
# Options: devnet, testnet, mainnet
# IMPORTANT: Use devnet or testnet for testing, mainnet for production only!
NETWORK=devnet

# RPC Endpoints (auto-configured based on NETWORK, but can override)
RPC_ENDPOINT=https://api.devnet.solana.com
WS_ENDPOINT=wss://api.devnet.solana.com

# For mainnet, use a private RPC (Helius, QuickNode, etc.)
# MAINNET_RPC=https://your-private-rpc-endpoint.com

# Database
DATABASE_URL=sqlite+aiosqlite:///./sniper.db

# Security
ENCRYPTION_KEY=your-super-secret-key-minimum-32-characters-long-change-this

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging
LOG_LEVEL=INFO
